version: "3.7"

services:
  selenium:
    image: selenium/standalone-chrome-debug:3.7.1
    ports:
      - 4444:4444
      - 5900:5900
  web:
    build: .
    volumes:
      - .:/code
    ports:
      - "8000:8000"
